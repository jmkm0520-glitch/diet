# 다이어트 식단 기록 웹사이트 구현 체크리스트

작성일: 2026-08-11
대상 범위: 단일 사용자 데모 MVP

## 참고 문서

- [기술 스택 문서](./TECH_STACK.md)
- [Notion — 다이어트 식단 기록 웹사이트 기획문서](https://app.notion.com/p/3b8cf891887c81d9bf80e1a2de604b51)

## 확정된 결정

- [x] 001. 결과물 체크리스트는 로컬 Markdown 파일로 관리한다.
- [x] 002. MVP는 회원가입과 로그인 없이 단일 사용자가 사용하는 데모로 구현한다.
- [x] 003. 같은 날짜와 같은 식사 종류를 다시 저장하면 기존 레코드를 덮어쓴다.
- [x] 004. 식단 기록 삭제 기능은 MVP에 포함하지 않는다.
- [x] 005. `clean.png`와 `free.png`는 Notion 목업 첨부파일의 캐릭터를 기반으로 준비한다.
- [x] 006. 프론트엔드는 Next.js App Router와 TypeScript를 사용한다.
- [x] 007. 백엔드는 Python 기반 Vercel Serverless Functions로 구성한다.
- [x] 008. 데이터베이스는 Supabase PostgreSQL을 사용한다.

## MVP 핵심 규칙

- 식사 종류는 `breakfast`, `lunch`, `dinner`, `snack`만 허용한다.
- 식단 구분은 `clean`, `free`만 허용한다.
- 해당 날짜에 기록된 식사가 모두 `clean`이면 `clean.png`를 표시한다. 네 끼가 모두 입력되지 않아도 같은 규칙을 적용한다.
- 해당 날짜에 `free` 식사가 하나라도 있으면 `free.png`를 표시한다.
- 체중만 있고 식단이 없는 날짜에는 상태 캐릭터를 표시하지 않는다.
- 체중은 kg 단위의 0보다 큰 숫자만 허용하며 날짜별 한 건만 유지한다.
- 클라이언트가 `YYYY-MM-DD` 형식의 날짜를 API에 명시적으로 전달하고, 서버의 UTC 날짜를 오늘 날짜로 사용하지 않는다.

## 1. 프로젝트 초기 설정

- [x] 009. Next.js App Router의 TypeScript 템플릿으로 프론트엔드 프로젝트를 생성한다.
- [x] 010. `api/` 디렉터리에 Python Vercel Functions 기본 구조를 만든다.
- [x] 011. `src/app`, `src/components`, `src/services`, `src/types`, `src/hooks` 디렉터리를 만든다.
- [x] 012. `supabase/migrations` 디렉터리를 만든다.
- [x] 013. 프론트엔드 의존성과 실행 스크립트를 `package.json`에 정리한다.
- [x] 014. Supabase Python Client, Pydantic 및 테스트 도구를 `requirements.txt`에 정의한다.
- [x] 015. ESLint와 Prettier를 설정하고 충돌하지 않는 포맷 규칙을 적용한다.
- [ ] 016. Python 린트 및 포맷 도구를 정하고 설정한다.
- [ ] 017. `.gitignore`에 `.env`, `.env.local`, 빌드 결과물 및 Python 캐시를 추가한다.
- [ ] 018. `.env.example`에 필요한 변수 이름과 용도를 작성한다.
  - [ ] 019. `SUPABASE_URL`
  - [ ] 020. `SUPABASE_SERVICE_ROLE_KEY`
- [ ] 021. MVP 프론트엔드는 Python API만 호출하므로 `NEXT_PUBLIC_SUPABASE_URL`과 `NEXT_PUBLIC_SUPABASE_ANON_KEY`를 번들에 넣지 않는다.
- [ ] 022. 향후 Supabase Auth 또는 Storage를 브라우저에서 직접 사용할 때만 `NEXT_PUBLIC_SUPABASE_URL`과 `NEXT_PUBLIC_SUPABASE_ANON_KEY`를 추가한다.
- [ ] 023. 프론트엔드와 Python API를 함께 배포하도록 `vercel.json`을 작성한다.
- [ ] 024. `/api/health` 상태 확인 API를 만들고 로컬 및 Vercel 환경에서 호출되는지 확인한다.

## 2. Notion 목업 및 이미지 자산 준비

- [ ] 025. Notion 기획문서의 `diet-tracker-screen-design-v2.html` 첨부파일을 확보한다.
- [ ] 026. Notion 기획문서의 `diet-status-emoji-gallery-v2.html` 첨부파일을 확보한다.
- [ ] 027. 첨부 HTML 내부에 캐릭터 원본 이미지가 포함되어 있는지 확인한다.
- [ ] 028. 원본 이미지가 있으면 해당 이미지 데이터를 추출하고, 없으면 목업을 고해상도로 렌더링하여 캐릭터를 분리한다.
- [ ] 029. 클린식 천사 캐릭터를 `public/clean.png`로 저장한다.
- [ ] 030. 자유식 악마 캐릭터를 `public/free.png`로 저장한다.
- [ ] 031. 두 PNG의 캔버스 크기, 캐릭터 비율, 여백 및 해상도를 동일하게 맞춘다.
- [ ] 032. 가능하면 배경을 투명하게 처리하고 작은 캘린더 셀에서도 외곽선과 표정이 선명한지 확인한다.
- [ ] 033. 목업의 아이보리·녹색·빨간색 계열을 디자인 토큰으로 정리한다.
- [ ] 034. 데스크톱 2열 식단 카드와 모바일 1열 배치를 목업과 대조한다.

## 3. Supabase 데이터베이스

### 3.1 식단 테이블

- [ ] 035. `meals` 테이블 생성 마이그레이션을 작성한다.
- [ ] 036. `id` UUID 기본 키를 추가한다.
- [ ] 037. `date` DATE 필드를 필수로 추가한다.
- [ ] 038. `meal` 필드에 `breakfast`, `lunch`, `dinner`, `snack`만 허용하는 제약 조건을 추가한다.
- [ ] 039. `food` 필드를 필수 텍스트로 추가하고 공백만 저장되지 않도록 API에서 검증한다.
- [ ] 040. `type` 필드에 `clean`, `free`만 허용하는 제약 조건을 추가한다.
- [ ] 041. `created_at`과 `updated_at` 타임스탬프를 추가한다.
- [ ] 042. `(date, meal)` 복합 UNIQUE 제약을 추가하여 식사별 한 건만 유지한다.
- [ ] 043. 날짜 범위 조회를 위한 `date` 인덱스를 추가한다.
- [ ] 044. 재저장 시 `(date, meal)`을 기준으로 upsert되는지 확인한다.

### 3.2 체중 테이블

- [ ] 045. `weights` 테이블 생성 마이그레이션을 작성한다.
- [ ] 046. `date`를 기본 키 또는 UNIQUE 필드로 지정하여 날짜별 한 건만 유지한다.
- [ ] 047. `weight`를 소수 저장이 가능한 숫자 타입으로 정의한다.
- [ ] 048. `weight > 0` 데이터베이스 제약 조건을 추가한다.
- [ ] 049. `created_at`과 `updated_at` 타임스탬프를 추가한다.
- [ ] 050. 재저장 시 `date`를 기준으로 upsert되는지 확인한다.

### 3.3 접근 제어

- [ ] 051. `meals`와 `weights` 테이블에 RLS를 활성화한다.
- [ ] 052. 브라우저용 anon key에 테이블 직접 읽기·쓰기 정책을 부여하지 않는다.
- [ ] 053. 데이터베이스 작업은 Python API에서만 수행하고 service role key는 서버 환경 변수에서만 읽는다.
- [ ] 054. 로그인 없는 단일 사용자 데모이므로 공개 API URL을 아는 사용자가 같은 데이터를 조회·수정할 수 있다는 제한을 README에 명시한다.
- [ ] 055. 외부 공개 전에 Vercel 배포 접근 제한 또는 인증 도입 필요 여부를 다시 검토한다.

## 4. Python API 및 비즈니스 로직

### 4.1 공통 처리

- [ ] 056. Supabase 클라이언트 초기화 모듈을 만들고 환경 변수 누락 시 안전하게 실패하도록 한다.
- [ ] 057. 요청·응답 Pydantic 모델을 작성한다.
- [ ] 058. 정상 응답을 `{ "data": ..., "error": null }` 형식으로 통일한다.
- [ ] 059. 오류 응답을 `{ "data": null, "error": { "code": ..., "message": ... } }` 형식으로 통일한다.
- [ ] 060. 잘못된 입력은 400, 없는 경로는 404, 내부 오류는 500 등 일관된 상태 코드를 사용한다.
- [ ] 061. 내부 예외, 토큰, 키 및 개인정보가 응답이나 로그에 포함되지 않도록 한다.
- [ ] 062. 날짜 입력이 실제 날짜인 `YYYY-MM-DD` 형식인지 검증한다.
- [ ] 063. 음식 입력의 앞뒤 공백을 제거하고 빈 문자열을 거부한다.
- [ ] 064. 요청 본문 크기를 제한한다.

### 4.2 일별 조회 API

- [ ] 065. `GET /api/day?date=YYYY-MM-DD`를 구현한다.
- [ ] 066. 응답에 선택 날짜, 체중 한 건 또는 `null`, 네 식사 슬롯을 포함한다.
- [ ] 067. 입력되지 않은 식사는 누락시키지 않고 프론트엔드가 `기록 없음`으로 표시할 수 있게 구분한다.
- [ ] 068. 식사와 체중이 모두 없는 날짜도 정상적인 빈 데이터로 반환한다.

### 4.3 식단 저장 API

- [ ] 069. `PUT /api/meal`을 구현한다.
- [ ] 070. `date`, `meal`, `food`, `type`을 검증한다.
- [ ] 071. `(date, meal)` 기준 upsert로 신규 저장과 덮어쓰기를 한 API에서 처리한다.
- [ ] 072. 저장 결과에 최종 레코드를 반환한다.
- [ ] 073. 동일한 요청을 반복해도 중복 레코드가 생기지 않는지 확인한다.

### 4.4 체중 저장 API

- [ ] 074. `PUT /api/weight`를 구현한다.
- [ ] 075. 빈 값, 문자열 혼합 값, `NaN`, 무한대 및 0 이하를 거부한다.
- [ ] 076. `date` 기준 upsert로 신규 저장과 수정을 처리한다.
- [ ] 077. 저장 결과에 최종 체중 레코드를 반환한다.

### 4.5 월별 캘린더 API

- [ ] 078. `GET /api/calendar?year=YYYY&month=MM`을 구현한다.
- [ ] 079. 요청 월의 시작일과 종료일을 안전하게 계산한다.
- [ ] 080. 날짜별 체중과 식단 상태를 한 번의 화면 요청에 사용할 수 있는 구조로 반환한다.
- [ ] 081. 기록된 식사가 모두 `clean`이면 날짜 상태를 `clean`으로 계산한다.
- [ ] 082. `free`가 하나라도 있으면 날짜 상태를 `free`로 계산한다.
- [ ] 083. 식단이 없으면 체중 유무와 관계없이 날짜 상태를 `null`로 반환한다.
- [ ] 084. 월 경계, 윤년 및 12월에서 1월로 이동하는 경우를 테스트한다.

## 5. 프론트엔드 공통 구조

- [ ] 085. `Meal`, `MealType`, `MealRecord`, `WeightRecord`, `DayRecord`, `CalendarDay` TypeScript 타입을 정의한다.
- [ ] 086. Fetch API 기반 서비스 모듈을 만들고 공통 응답·오류 형식을 처리한다.
- [ ] 087. API 로딩, 성공 및 실패 상태를 화면에서 일관되게 표현한다.
- [ ] 088. 날짜 문자열 유틸리티를 만들고 브라우저 로컬 날짜가 하루 앞뒤로 바뀌지 않는지 확인한다.
- [ ] 089. 상단 왼쪽에 로고와 서비스명 `오늘도 가볍게`를 배치한다.
- [ ] 090. 상단 오른쪽에 `오늘 기록`과 `캘린더` 탭을 구현한다.
- [ ] 091. 탭과 선택 날짜를 URL 또는 명시적인 앱 상태로 관리하여 수정하기 이동 시 날짜를 유지한다.
- [ ] 092. 본문 최대 너비, 아이보리 배경 및 공통 간격을 적용한다.
- [ ] 093. 색상, 테두리, 그림자, 폰트 크기 및 반응형 분기점을 CSS 변수로 정의한다.
- [ ] 094. 키보드 포커스가 보이고 버튼에 접근 가능한 이름이 제공되도록 한다.

## 6. 오늘 기록 화면

### 6.1 날짜 이동

- [ ] 095. 기본 선택 날짜를 브라우저 기준 오늘로 설정한다.
- [ ] 096. 선택 날짜를 큰 제목과 안내 문구로 표시한다.
- [ ] 097. 이전 날짜 및 다음 날짜 버튼을 구현한다.
- [ ] 098. 날짜 선택 입력을 구현한다.
- [ ] 099. 날짜가 바뀌면 해당 날짜의 체중과 식단 기록을 다시 불러온다.
- [ ] 100. 캘린더 상세의 수정하기·기록하기 이동 시 선택 날짜를 그대로 표시한다.

### 6.2 체중 입력

- [ ] 101. 체중 영역을 녹색 계열의 강조 카드로 구현한다.
- [ ] 102. 숫자 입력창, `kg` 단위 및 저장 버튼을 한 줄에 배치한다.
- [ ] 103. 소수점 입력을 허용한다.
- [ ] 104. 빈 값, 문자가 섞인 값 및 0 이하 값에 인라인 오류를 표시한다.
- [ ] 105. 기존 체중이 있으면 입력창과 저장된 값에 반영한다.
- [ ] 106. 저장 중 중복 클릭을 막고 로딩 상태를 표시한다.
- [ ] 107. 저장 성공 후 `저장됨 ✓` 피드백을 표시한다.
- [ ] 108. 같은 날짜에 다시 저장하면 화면과 데이터베이스의 기존 값이 변경되는지 확인한다.

### 6.3 식단 카드

- [ ] 109. 아침, 점심, 저녁, 간식 카드를 공통 컴포넌트로 구현한다.
- [ ] 110. 데스크톱에서는 2열, 모바일에서는 1열로 배치한다.
- [ ] 111. 각 카드에 식사명, 상태, 음식 입력창, 종류 토글 및 개별 저장 버튼을 배치한다.
- [ ] 112. 클린식과 자유식 토글 중 하나만 선택되도록 한다.
- [ ] 113. 클린식 선택 시 초록색 테두리와 상태 문구를 즉시 표시한다.
- [ ] 114. 자유식 선택 시 빨간색 테두리와 상태 문구를 즉시 표시한다.
- [ ] 115. 음식 또는 식단 종류가 없으면 저장하지 않고 인라인 오류를 표시한다.
- [ ] 116. 저장 중인 카드만 비활성화하여 다른 식사 입력은 계속 가능하게 한다.
- [ ] 117. 저장 성공 후 해당 카드에 `저장됨 ✓` 피드백을 표시한다.
- [ ] 118. 기존 식사를 다시 저장하면 새 카드가 추가되지 않고 해당 카드 내용이 갱신되는지 확인한다.
- [ ] 119. 날짜를 이동했다가 돌아와도 저장된 네 식사와 체중이 다시 표시되는지 확인한다.

## 7. 캘린더 화면

### 7.1 월별 캘린더

- [ ] 120. 일요일부터 토요일까지 요일 헤더를 표시한다.
- [ ] 121. 이전 달, 현재 연월 및 다음 달 컨트롤을 구현한다.
- [ ] 122. 월 시작 요일과 마지막 날짜를 반영하여 날짜 셀을 정확히 배치한다.
- [ ] 123. 클린식과 자유식 범례를 표시한다.
- [ ] 124. 오늘 날짜를 연한 녹색 배경으로 강조한다.
- [ ] 125. 클린 날짜 셀에 `clean.png`를 표시한다.
- [ ] 126. 자유식 포함 날짜 셀에 `free.png`를 표시한다.
- [ ] 127. 식단 기록이 없는 날짜에는 캐릭터를 표시하지 않는다.
- [ ] 128. 데스크톱 날짜 셀에 저장된 체중을 표시한다.
- [ ] 129. 모바일에서는 체중 텍스트를 숨기고 캐릭터 가독성을 우선한다.
- [ ] 130. 이미지에 `클린식 날짜`, `자유식 포함 날짜` 대체 텍스트를 제공한다.
- [ ] 131. 월 이동 시 기존 데이터가 섞이지 않도록 로딩 및 빈 상태를 처리한다.

### 7.2 날짜별 상세 패널

- [ ] 132. 날짜 셀 전체를 버튼 또는 동등한 키보드 조작 가능 요소로 구현한다.
- [ ] 133. 날짜 선택 시 해당 날짜의 일별 데이터를 불러온다.
- [ ] 134. 데스크톱에서는 캘린더 오른쪽 또는 하단 패널로 표시한다.
- [ ] 135. 모바일에서는 하단 바텀 시트로 표시한다.
- [ ] 136. 패널 상단에 선택 날짜, 상태 캐릭터 및 체중을 표시한다.
- [ ] 137. 아침, 점심, 저녁, 간식을 항상 정해진 순서로 표시한다.
- [ ] 138. 입력되지 않은 식사는 `기록 없음`으로 표시한다.
- [ ] 139. 기록이 있는 날짜에는 `수정하기` 버튼을 표시한다.
- [ ] 140. 식사와 체중이 모두 없는 날짜에는 `아직 기록이 없어요`와 `기록하기` 버튼을 표시한다.
- [ ] 141. 수정하기 또는 기록하기를 누르면 선택 날짜의 기록 화면으로 이동한다.
- [ ] 142. 닫기 버튼, 패널 바깥 영역 및 ESC 키로 패널을 닫을 수 있게 한다.
- [ ] 143. 패널을 닫은 뒤 선택했던 날짜 셀로 키보드 포커스를 되돌린다.

## 8. 테스트 및 품질 검증

### 8.1 백엔드 테스트

- [ ] 144. 날짜, 식사 종류, 식단 구분 및 음식 입력 검증을 단위 테스트한다.
- [ ] 145. 유효 체중과 잘못된 체중 입력을 단위 테스트한다.
- [ ] 146. 식사 저장 후 같은 `(date, meal)` 재저장이 덮어쓰는지 통합 테스트한다.
- [ ] 147. 같은 날짜의 체중 재저장이 덮어쓰는지 통합 테스트한다.
- [ ] 148. 전체 클린, 일부 식사만 클린, 자유식 포함, 식단 없음의 하루 판정 규칙을 테스트한다.
- [ ] 149. 데이터베이스 오류가 표준 오류 응답으로 변환되는지 테스트한다.

### 8.2 프론트엔드 테스트

- [ ] 150. 식단 종류 선택에 따라 카드 테두리가 즉시 바뀌는지 테스트한다.
- [ ] 151. 식사 카드별 독립 입력과 저장 상태를 테스트한다.
- [ ] 152. 체중과 식단 입력 오류 메시지를 테스트한다.
- [ ] 153. 날짜 이동 시 API 재조회와 화면 갱신을 테스트한다.
- [ ] 154. 캘린더 상태 캐릭터 선택 규칙을 테스트한다.
- [ ] 155. 상세 패널의 빈 상태, 수정하기, 기록하기 및 닫기 동작을 테스트한다.

### 8.3 수동·반응형 검증

- [ ] 156. 모바일, 태블릿 및 데스크톱 너비에서 레이아웃을 확인한다.
- [ ] 157. 모바일에서 식단 카드가 1열이고 캘린더 체중이 숨겨지는지 확인한다.
- [ ] 158. 키보드만으로 탭, 날짜 이동, 식단 저장 및 상세 패널 조작이 가능한지 확인한다.
- [ ] 159. 로딩 중 레이아웃이 과도하게 흔들리지 않는지 확인한다.
- [ ] 160. 긴 음식명이 카드와 상세 패널을 깨뜨리지 않는지 확인한다.
- [ ] 161. API 오류 후 사용자가 다시 시도할 수 있는지 확인한다.

## 9. 보안 및 운영 점검

- [ ] 162. service role key가 프론트엔드 번들, Git 기록 및 로그에 포함되지 않았는지 검사한다.
- [ ] 163. Vercel의 개발·미리보기·운영 환경 변수를 분리한다.
- [ ] 164. 모든 API에서 입력값을 서버 측에서도 다시 검증한다.
- [ ] 165. 상세 예외 대신 사용자용 오류 메시지만 반환한다.
- [ ] 166. 로그에 음식 기록 등 사용자 입력을 불필요하게 남기지 않는다.
- [ ] 167. Supabase 백업 및 복구 방법을 운영 전 확인한다.
- [ ] 168. 구조화된 API 오류 로그를 남기되 비밀 정보는 제거한다.

## 10. 배포 및 최종 완료 확인

- [ ] 169. TypeScript 타입 검사, ESLint, Prettier 검사를 통과한다.
- [ ] 170. Python 린트와 백엔드 테스트를 통과한다.
- [ ] 171. 프론트엔드 테스트를 통과한다.
- [ ] 172. 프로덕션 빌드가 성공하는지 확인한다.
- [ ] 173. Supabase 마이그레이션을 대상 프로젝트에 적용한다.
- [ ] 174. Vercel Preview에서 프론트엔드, Python API 및 Supabase 연결을 확인한다.
- [ ] 175. HTTPS 환경에서 혼합 콘텐츠 오류가 없는지 확인한다.
- [ ] 176. 운영 배포 후 `/api/health`를 확인한다.
- [ ] 177. 운영 배포에서 실제 식사와 체중의 저장·덮어쓰기·재조회 흐름을 확인한다.
- [ ] 178. 월별 캘린더의 이전 달·다음 달 이동과 날짜 상세 조회를 확인한다.

## MVP 완료 조건

- [ ] 179. 날짜별 체중을 입력·저장·수정하고 다시 확인할 수 있다.
- [ ] 180. 빈 값, 문자가 포함된 값 및 0 이하의 체중은 저장되지 않는다.
- [ ] 181. 네 가지 식사를 각각 독립적으로 입력하고 저장할 수 있다.
- [ ] 182. 같은 날짜와 식사 종류를 다시 저장하면 기존 기록이 수정되고 중복되지 않는다.
- [ ] 183. 저장된 카드의 테두리 색상이 식단 종류와 정확히 일치한다.
- [ ] 184. 날짜를 이동해도 해당 날짜의 기록을 다시 확인할 수 있다.
- [ ] 185. 월별 캘린더에 하루 판정 규칙과 일치하는 캐릭터가 표시된다.
- [ ] 186. 자유식이 하나라도 포함된 날짜에는 반드시 `free.png`가 표시된다.
- [ ] 187. 식단 기록이 없는 날짜에는 캐릭터가 표시되지 않는다.
- [ ] 188. 기록된 날짜를 선택하면 체중과 모든 식사 기록이 표시된다.
- [ ] 189. 기록이 없는 날짜를 선택하면 빈 상태와 기록하기 버튼이 표시된다.
- [ ] 190. 상세 패널의 수정하기 버튼이 선택 날짜의 기록 화면으로 이동한다.
- [ ] 191. 모바일과 데스크톱에서 핵심 기능을 사용할 수 있다.

## MVP 제외 및 향후 작업

- [ ] 192. 회원가입, 로그인 및 사용자별 RLS 정책
- [ ] 193. 식단 기록 삭제
- [ ] 194. 칼로리 및 영양성분 자동 계산
- [ ] 195. 체중 변화 선 그래프
- [ ] 196. 월별 클린식 통계와 달성률
- [ ] 197. 연속 클린식 기록
- [ ] 198. 식단 사진 업로드 및 Supabase Storage 연동
- [ ] 199. AI 식단 분석과 추천
- [ ] 200. 친구 기능 및 SNS 공유
