# 다이어트 식단 기록 웹사이트 체크리스트

작성일: 2026-08-11
쉬운 말 개편: 2026-08-12
대상: 로그인 없이 한 사람이 쓰는 데모 웹사이트

> 이 문서는 웹사이트를 만들기 위한 할 일 목록입니다. 위에서 아래 순서로 진행합니다.

## 읽는 법

- `[x]`는 끝난 일, `[ ]`는 아직 할 일입니다.
- 번호 하나는 작은 작업 하나입니다.
- **데이터베이스**는 기록을 보관하는 서랍, **API**는 화면과 서랍을 연결하는 창구입니다.
- **테스트**는 기능이 제대로 되는지 자동으로 확인하는 검사입니다.
- **업서트(upsert)**는 처음에는 저장하고, 같은 자리에 이미 기록이 있으면 수정하는 방식입니다.

## 전체 흐름

| 단계 | 하는 일                                   | 상태    |
| ---- | ----------------------------------------- | ------- |
| 0~2  | 웹사이트 기본 틀, 디자인, 안전장치 만들기 | 완료    |
| 3    | 날짜별 기록을 불러오는 기능 만들기        | 완료    |
| 4    | 체중 입력·수정 기능 만들기                | 완료    |
| 5    | 아침·점심·저녁·간식 입력 기능 만들기      | 진행 중 |
| 6~7  | 캘린더와 날짜 상세 보기 만들기            | 예정    |
| 8~10 | 모바일 점검, 보안, 배포 확인              | 예정    |

## 참고 문서

- [기술 스택 문서](./TECH_STACK.md)
- [Notion 기획문서](https://app.notion.com/p/3b8cf891887c81d9bf80e1a2de604b51)

## 정해진 기본 규칙

- [x] 001. 결과물 체크리스트를 로컬 Markdown 파일로 관리한다. (작업 목록 파일이라는 뜻)
- [x] 002. MVP는 회원가입·로그인 없이 한 사람이 쓰는 데모로 구현한다. (MVP는 꼭 필요한 기능만 있는 첫 버전)
- [x] 003. 같은 날짜와 같은 식사를 다시 저장하면 기존 레코드를 덮어쓴다. (레코드는 저장된 기록 한 줄)
- [x] 004. 첫 버전에는 식단 기록 삭제 기능을 넣지 않는다. (실수로 기록을 지우는 기능은 나중에 만든다)
- [x] 005. `clean.png`(천사)와 `free.png`(악마) 캐릭터 이미지를 준비한다.
- [x] 006. 프론트엔드는 Next.js App Router와 TypeScript를 사용한다. (프론트엔드는 사용자가 보는 화면)
- [x] 007. 백엔드는 Python 기반 Vercel Serverless Functions로 구성한다. (백엔드는 저장을 처리하는 서버)
- [x] 008. 데이터베이스는 Supabase PostgreSQL을 사용한다. (기록을 보관하는 곳)

## 꼭 지킬 기록 규칙

- 식사 시간은 아침·점심·저녁·간식 네 개만 사용한다.
- 식단 종류는 클린식(`clean`)과 자유식(`free`)만 사용한다.
- 식사가 모두 클린식이면 천사 캐릭터를 표시한다.
- 자유식이 하나라도 있으면 악마 캐릭터를 표시한다.
- 체중만 있고 식단이 없으면 캐릭터를 표시하지 않는다.
- 체중은 0보다 큰 kg 숫자만 저장하고, 날짜마다 한 개만 둔다.
- 날짜는 서버 시간이 아니라 사용자가 고른 날짜를 사용한다.

## 0. 웹사이트 기본 틀 만들기

목표: 개발·검사·배포할 수 있는 기본 환경을 준비한다.

- [x] 009. Next.js App Router의 TypeScript 템플릿으로 기본 프로젝트를 만든다. (웹사이트의 빈 뼈대를 준비한다)
- [x] 010. `api/` 디렉터리에 Python Vercel Functions 기본 구조를 만든다. (서버 기능을 넣을 자리를 준비한다)
- [x] 011. `src/app`, `src/components`, `src/services`, `src/types`, `src/hooks` 디렉터리를 만든다. (화면·공통부품·데이터처리 등을 나눠 담는 폴더)
- [x] 012. `supabase/migrations` 디렉터리를 만든다. (데이터베이스 설계도 파일을 담는 폴더)
- [x] 013. 프론트엔드 의존성과 실행 스크립트를 `package.json`에 정리한다. (웹사이트 실행에 필요한 도구 목록)
- [x] 014. Supabase Python Client, Pydantic, 테스트 도구를 `requirements.txt`에 정의한다. (Python 서버 실행에 필요한 도구 목록)
- [x] 015. ESLint와 Prettier를 설정한다. (코드 문제를 찾고 모양을 맞추는 도구)
- [x] 016. Python 린트 및 포맷 도구를 설정한다. (린트는 코드 문제 검사, 포맷은 코드 모양 정리)
- [x] 017. `.gitignore`에 `.env`, 빌드 결과물, Python 캐시를 추가한다. (GitHub에 올리지 않을 파일 목록)
- [x] 018. `.env.example`에 필요한 환경 변수 이름과 용도를 작성한다. (비밀번호 자체가 아닌 이름만 적은 예시 파일)
  - [x] 019. 데이터베이스 주소 설정값을 적는다.
  - [x] 020. 서버 전용 비밀키 설정값을 적는다.
- [x] 021. MVP 브라우저 번들에는 `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`를 넣지 않는다. (번들은 사용자에게 전달되는 화면 코드)
- [x] 022. Supabase Auth 또는 Storage를 브라우저에서 직접 쓸 때만 `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`를 추가한다. (로그인·파일 저장 기능이 필요해질 때만 공개 설정을 쓴다)
- [x] 023. Next.js와 Python API를 함께 배포하도록 `vercel.json`을 작성한다. (화면과 서버를 인터넷에 함께 올리는 설정)
- [x] 024. `/api/health` 상태 확인 API를 만들고 호출되는지 확인한다. (서버가 정상인지 확인하는 주소)

## 1. 디자인과 캐릭터 준비

목표: 공통 색상·레이아웃과 천사·악마 캐릭터를 준비한다.

- [x] 025. Notion에서 화면 디자인 참고 파일을 가져온다. (원하는 화면 모양을 확인한다)
- [x] 026. Notion에서 상태 캐릭터 참고 파일을 가져온다. (천사·악마 캐릭터 모양을 확인한다)
- [x] 027. 참고 파일 안에 원본 캐릭터 이미지가 있는지 확인한다.
- [x] 028. 원본이 없으면 참고 화면에서 캐릭터 이미지를 만든다.
- [x] 029. 천사 캐릭터를 `clean.png`로 저장한다.
- [x] 030. 악마 캐릭터를 `free.png`로 저장한다.
- [x] 031. 두 캐릭터의 크기·여백·화질을 맞춘다.
- [x] 032. 캐릭터 배경을 투명하게 하고 작게 보여도 잘 보이는지 확인한다.
- [x] 033. 목업의 아이보리·녹색·빨간색 계열을 디자인 토큰으로 정리한다. (디자인 토큰은 공통 색상·크기 이름)
- [x] 034. 데스크톱 2열 식단 카드와 모바일 1열 배치를 목업과 대조한다.
- [x] 035. 본문 최대 너비, 간격, 테두리, 그림자, 글자 크기, 반응형 분기점을 CSS 변수로 정의한다.
- [x] 036. 왼쪽 위에 브로콜리 로고와 서비스 이름을 넣는다. (웹사이트 이름을 보여 준다)
- [x] 037. 오른쪽 위에 오늘 기록·캘린더 탭을 만든다. (두 화면을 이동하는 버튼)
- [x] 038. 탭의 현재 선택 상태가 시각적으로 구분되고 키보드로 이동되는지 확인한다.

## 2. 화면과 서버를 안전하게 연결하기

목표: 모든 기능이 같은 방식으로 데이터를 주고받고, 오류를 안전하게 보여 준다.

- [x] 039. Supabase 클라이언트 초기화 모듈을 만들고 환경 변수 누락 시 안전하게 실패하게 한다. (설정이 없을 때 엉뚱한 저장을 막는다)
- [x] 040. 정상 응답을 `{ "data": ..., "error": null }` 형식으로 통일한다.
- [x] 041. 오류 응답을 `{ "data": null, "error": { "code": ..., "message": ... } }` 형식으로 통일한다.
- [x] 042. 잘못된 입력은 400, 없는 경로는 404, 내부 오류는 500처럼 일관된 상태 코드를 사용한다.
- [x] 043. Fetch API 기반 서비스 모듈을 만들고 공통 응답·오류 형식을 처리한다.
- [x] 044. API 로딩, 성공, 실패 상태를 화면에서 일관되게 표현하는 공통 컴포넌트를 만든다.
- [x] 045. 서버에서 날짜 형식이 맞는지 검사한다. (예: 2026-08-12처럼 올바른 날짜인지 확인)
- [x] 046. 브라우저 로컬 날짜를 `YYYY-MM-DD`로 안전하게 바꾸는 TypeScript 유틸리티를 만든다.
- [x] 047. UTC 때문에 날짜가 하루 앞뒤로 바뀌지 않는지 테스트한다. (UTC는 세계 공통 시간)
- [x] 048. 요청 본문 크기를 제한한다.
- [x] 049. 오류 화면과 로그에 비밀키·개인정보가 나오지 않게 한다. (비밀번호나 기록 내용이 새지 않게 함)

## 3. 날짜를 고르고 그날 기록 보기

목표: 사용자가 날짜를 바꾸면 그날의 체중과 네 끼 기록을 불러온다.

- [x] 050. `Meal`, `MealType`, `MealRecord`, `WeightRecord`, `DayRecord` TypeScript 타입을 정의한다. (타입은 데이터 모양을 미리 정하는 규칙)
- [x] 051. 일별 응답용 Pydantic 모델을 작성한다. (Pydantic은 서버 입력·출력 형식 검사 도구)
- [x] 052. `GET /api/day?date=YYYY-MM-DD`를 구현한다. (`GET`은 기록을 가져오는 요청)
- [x] 053. 하루 응답에 날짜·체중·네 식사 자리를 모두 넣는다. (화면이 한 번에 필요한 정보를 받는다)
- [x] 054. 기록하지 않은 식사도 빈 자리로 항상 보여 준다. (아침·점심·저녁·간식 카드가 늘 같은 순서로 보임)
- [x] 055. 아무 기록이 없는 날도 오류 없이 빈 화면으로 돌려준다.
- [x] 056. 처음 열면 브라우저 기준 오늘 날짜를 선택한다.
- [x] 057. 선택한 날짜를 큰 제목으로 보여 준다.
- [x] 058. 이전 날짜·다음 날짜 버튼을 만든다.
- [x] 059. 달력 입력으로 날짜를 직접 고를 수 있게 한다.
- [x] 060. 날짜를 바꾸면 새 날짜의 기록을 다시 불러온다. (고른 날짜에 맞는 기록을 보여 줌)
- [x] 061. 날짜를 빨리 바꿔도 이전 날짜 기록이 섞이지 않게 한다.
- [x] 062. 잘못된 날짜와 빈 날짜를 테스트한다.
- [x] 063. 다른 날짜로 갔다가 돌아오면 저장 기록이 다시 보이는지 테스트한다.

## 4. 체중 기록

목표: 날짜별 체중을 저장하고 같은 날짜의 값을 수정한다.

### 체중을 저장할 서랍 만들기

- [x] 064. `weights` 테이블 생성 마이그레이션을 작성한다. (체중을 저장할 데이터베이스 표를 만든다)
- [x] 065. `date`를 기본 키 또는 UNIQUE 필드로 지정해 날짜별 한 건만 유지한다.
- [x] 066. `weight`를 소수 저장이 가능한 숫자 타입으로 정의한다.
- [x] 067. `weight > 0` 데이터베이스 제약 조건을 추가한다.
- [x] 068. `created_at`, `updated_at` 타임스탬프를 추가한다. (생성·마지막 수정 시간)
- [x] 069. 체중 요청·응답 Pydantic 모델을 작성한다. (체중 데이터 모양을 서버에서 검사하는 규칙)
- [x] 070. `PUT /api/weight`를 구현한다. (`PUT`은 저장하거나 기존 값을 수정하는 요청)
- [x] 071. 빈 값, 문자열 혼합 값, `NaN`, 무한대, 0 이하 값을 서버에서 거부한다.
- [x] 072. `date` 기준 upsert로 신규 저장과 수정을 처리한다.
- [x] 073. 저장 결과에 최종 체중 레코드를 반환한다.

### 체중 화면 만들기

- [x] 074. 체중 입력 부분을 초록색 강조 카드로 만든다. (체중 기록 영역이 눈에 잘 띄게 함)
- [x] 075. 숫자칸·kg·저장 버튼을 한 줄에 둔다.
- [x] 076. 소수점 체중을 입력할 수 있게 한다.
- [x] 077. 잘못 입력하면 입력칸 아래에 이유를 보여 준다.
- [x] 078. 이미 저장한 체중은 입력칸에 다시 보여 준다.
- [x] 079. 저장 중에는 저장 버튼을 여러 번 누르지 못하게 한다.
- [x] 080. 저장되면 숫자칸을 잠그고 버튼을 `수정`으로 바꾼다.
- [x] 081. 저장에 실패해도 입력한 숫자를 지우지 않고 다시 시도하게 한다. (다시 입력하는 수고를 줄임)

### 체중 기능 검사

- [x] 082. 정수 체중과 소수 체중이 저장되는지 검사한다.
- [x] 083. 빈값·문자·NaN·무한대·0 이하를 막는지 검사한다.
- [x] 084. 같은 날짜 체중을 다시 저장하면 값이 바뀌는지 검사한다.
- [x] 085. 한 날짜 체중을 바꿔도 다른 날짜 체중은 그대로인지 검사한다.

## 5. 식단 기록

목표: 아침·점심·저녁·간식을 각각 저장하고 클린식·자유식으로 표시한다.

### 식단을 저장할 서랍 만들기

- [x] 086. `meals` 테이블 생성 마이그레이션을 작성한다. (아침·점심·저녁·간식 기록을 저장할 표를 만든다)
- [x] 087. `id` UUID 기본 키를 추가한다. (UUID는 겹치지 않는 긴 고유 번호)
- [x] 088. `date` DATE 필드를 필수로 추가한다. (어느 날짜의 식단인지 반드시 저장)
- [x] 089. `meal`에는 `breakfast`, `lunch`, `dinner`, `snack`만 허용하는 제약 조건을 추가한다.
- [x] 090. `food`를 필수 텍스트 필드로 추가한다. (무엇을 먹었는지 꼭 적게 함)
- [x] 091. `type`에는 `clean`, `free`만 허용하는 제약 조건을 추가한다.
- [x] 092. `created_at`, `updated_at` 타임스탬프를 추가한다. (처음 저장·마지막 수정 시간을 남김)
- [x] 093. `(date, meal)` 복합 UNIQUE 제약을 추가한다. (날짜와 식사 조합이 같으면 한 건만 저장)
- [x] 094. 날짜 범위 조회를 위한 `date` 인덱스를 추가한다. (인덱스는 더 빨리 찾는 검색표)
- [x] 095. 식단 요청·응답 Pydantic 모델을 작성한다. (식단 저장에 필요한 날짜·식사·음식·종류를 검사)
- [x] 096. `PUT /api/meal`을 구현한다. (식단을 저장하거나 고치는 서버 주소)
- [x] 097. `date`, `meal`, `food`, `type`을 서버에서 검증한다.
- [x] 098. 음식 입력의 앞뒤 공백을 제거하고 공백만 있는 값을 거부한다.
- [x] 099. `(date, meal)` 기준 upsert로 신규 저장과 덮어쓰기를 처리한다.
- [x] 100. 저장 결과에 최종 식단 레코드를 반환한다.

### 식단 화면 만들기

- [x] 101. 아침·점심·저녁·간식 카드를 공통 React 컴포넌트로 구현한다. (한 카드 부품을 네 번 재사용)
- [x] 102. 데스크톱은 2열, 모바일은 1열 CSS 레이아웃으로 배치한다.
- [x] 103. 카드마다 식사명·상태·`food` 입력창·`type` 토글·개별 저장 버튼을 둔다.
- [x] 104. `clean`과 `free` 토글 중 하나만 선택되도록 한다. (클린식·자유식 둘 중 하나만 고르게 함)
- [x] 105. `clean` 토글을 선택하면 초록색 테두리와 상태 문구를 즉시 표시한다.
- [x] 106. `free` 토글을 선택하면 빨간색 테두리와 상태 문구를 즉시 표시한다.
- [x] 107. `food` 또는 `type`이 없으면 저장하지 않고 인라인 오류를 표시한다. (인라인 오류는 해당 카드 바로 아래 안내)
- [x] 108. 저장 중인 카드만 비활성화하고 다른 식사 입력은 계속 가능하게 한다. (한 끼 저장 중에도 다른 끼는 계속 기록 가능)
- [x] 109. 저장 성공 후 음식 입력칸을 비활성화하고 버튼을 `수정`으로 바꾼다.
- [x] 110. 저장 실패 시 해당 카드의 입력값을 유지하고 다시 시도할 수 있게 한다. (입력한 음식이 사라지지 않게 함)

### 식단 기능 검사

- [x] 111. 네 끼를 각각 따로 저장할 수 있는지 검사한다.
- [x] 112. 잘못된 식사·식단 종류·빈 음식은 막는지 검사한다.
- [x] 113. 식단 종류를 고르면 카드 색이 바로 바뀌는지 검사한다.
- [x] 114. 같은 날짜의 같은 식사를 다시 저장하면 기존 값만 바뀌는지 검사한다.
- [x] 115. 한 카드 저장 중에도 다른 카드 입력이 가능한지 검사한다.
- [x] 116. 날짜를 옮겼다가 돌아와도 저장한 식단이 보이는지 검사한다. (날짜별로 기록이 잘 보관되는지 확인)

## 6. 월별 캘린더와 하루 상태

목표: 한 달 기록을 보고, 식단 상태에 맞는 천사·악마 캐릭터를 날짜에 표시한다.

- [x] 117. `CalendarDay` TypeScript 타입과 월별 응답 Pydantic 모델을 작성한다. (달력 하루 칸에 담을 정보의 모양을 정함)
- [x] 118. `GET /api/calendar?year=YYYY&month=MM`을 구현한다. (선택한 달의 기록을 가져오는 서버 주소)
- [x] 119. 요청 월의 시작일과 종료일을 안전하게 계산한다. (예: 2월은 28일 또는 29일인지 정확히 찾기)
- [x] 120. 날짜별 체중과 식단 상태를 한 번의 화면 요청에 쓸 수 있는 구조로 반환한다. (달력을 그리는 데 필요한 정보를 한 번에 보냄)
- [x] 121. 기록된 식사가 모두 `clean`이면 날짜 상태를 `clean`으로 계산한다. (그날을 천사 상태로 정함)
- [x] 122. 기록된 식사 중 `free`가 하나라도 있으면 날짜 상태를 `free`로 계산한다. (자유식 하나만 있어도 악마 상태로 정함)
- [x] 123. 식단이 없으면 체중 유무와 관계없이 날짜 상태를 `null`로 반환한다. (체중만 기록한 날에는 캐릭터를 보이지 않게 함)
- [x] 124. 일요일부터 토요일까지 요일 헤더를 표시한다. (달력 맨 위의 요일 글자)
- [x] 125. 이전 달·현재 연월·다음 달 컨트롤을 구현한다. (보고 싶은 달로 이동하는 버튼)
- [x] 126. 월 시작 요일과 마지막 날짜를 반영해 날짜 셀을 정확히 배치한다. (달력 날짜가 요일에 맞게 놓이도록 함)
- [x] 127. `clean`과 `free` 범례를 표시한다. (범례는 기호 뜻을 알려주는 설명)
- [x] 128. 오늘 날짜를 연한 녹색 배경으로 강조한다. (오늘이 어디인지 바로 알 수 있게 함)
- [x] 129. `clean` 날짜 셀에 `clean.png`를 표시한다.
- [x] 130. `free`가 포함된 날짜 셀에 `free.png`를 표시한다.
- [x] 131. 식단 기록이 없는 날짜에는 캐릭터를 표시하지 않는다.
- [x] 132. 데스크톱 날짜 셀에 저장된 체중을 표시한다.
- [ ] 133. 모바일에서는 체중 텍스트를 숨기고 캐릭터 가독성을 우선한다.
- [x] 134. 이미지에 `클린식 날짜`, `자유식 포함 날짜` 대체 텍스트(alt)를 제공한다.
- [x] 135. 월 이동 시 이전 월 데이터가 섞이지 않도록 로딩과 빈 상태를 처리한다. (달을 빨리 바꿔도 기록이 뒤섞이지 않게 함)
- [x] 136. 일부만 기록해도 모두 클린식이면 천사인지 검사한다.
- [x] 137. 자유식이 하나라도 있으면 반드시 악마인지 검사한다.
- [x] 138. 체중만 있거나 빈 날에는 캐릭터가 없는지 검사한다.
- [x] 139. 윤년·연말·연초 등 달력 날짜 배치가 맞는지 검사한다.
- [ ] 140. 모바일에서 체중 글자가 숨겨지는지 검사한다.

## 7. 날짜를 눌러 자세히 보기

목표: 달력에서 날짜를 눌러 그날 기록을 보고, 바로 수정 화면으로 이동한다.

- [x] 141. 날짜 셀 전체를 키보드로도 선택할 수 있는 요소로 구현한다. (마우스 없이도 날짜를 고를 수 있게 함)
- [x] 142. 날짜를 선택하면 해당 날짜의 일별 데이터를 불러온다. (누른 날짜의 체중·식단을 보여 줌)
- [x] 143. 데스크톱에서는 캘린더 오른쪽 또는 아래에 상세 패널을 표시한다.
- [x] 144. 모바일에서는 상세 패널을 바텀 시트로 표시한다. (바텀 시트는 아래에서 올라오는 창)
- [x] 145. 패널 상단에 선택 날짜·상태 캐릭터·체중을 표시한다.
- [x] 146. 아침·점심·저녁·간식을 정해진 순서로 표시한다.
- [x] 147. 입력하지 않은 식사는 `기록 없음`으로 표시한다.
- [x] 148. 기록이 있는 날짜에는 `수정하기` 버튼을 표시한다.
- [x] 149. 식사와 체중이 모두 없는 날짜에는 `아직 기록이 없어요`와 `기록하기` 버튼을 표시한다.
- [x] 150. 탭과 선택 날짜를 URL 또는 명시적인 앱 상태로 관리한다. (화면을 옮겨도 보고 있던 날짜를 잃지 않게 함)
- [x] 151. 수정하기·기록하기를 누르면 선택 날짜의 오늘 기록 화면으로 이동한다.
- [x] 152. 이동 뒤 선택 날짜의 기존 기록을 입력 폼에 표시한다.
- [x] 153. 닫기 버튼·패널 바깥 클릭·ESC 키로 패널을 닫을 수 있게 한다.
- [x] 154. 패널을 닫은 뒤 키보드 포커스를 원래 날짜 셀로 되돌린다.
- [x] 155. 기록 있음·체중만 있음·빈 날짜의 화면을 각각 검사한다.
- [x] 156. 수정·기록 화면으로 이동해도 선택한 날짜가 유지되는지 검사한다.
- [x] 157. 자세히 보기 창을 닫는 모든 방법을 검사한다.

## 8. 모바일·사용성·접근성 점검

목표: 휴대폰과 PC에서 누구나 편하게 사용할 수 있게 다듬는다.

- [x] 158. 모바일·태블릿·데스크톱 너비에서 전체 흐름을 확인한다. (화면 크기가 달라도 사용 가능한지 확인)
- [x] 159. 모바일에서 식단 카드가 1열인지 확인한다.
- [x] 160. 키보드만으로 탭·날짜 이동·체중·식단 저장·상세 패널을 조작할 수 있는지 확인한다. (마우스 없이도 모든 기능을 쓸 수 있는지 확인)
- [x] 161. 모든 버튼과 입력 요소에 접근 가능한 이름(aria-label)을 제공한다.
- [x] 162. 키보드 포커스가 시각적으로 명확한지 확인한다.
- [x] 163. 상태를 색상으로만 전달하지 않고 문구나 이미지 대체 텍스트를 함께 제공한다.
- [x] 164. 저장 성공·실패 메시지를 보조 기술(화면 읽기 도구)도 인식하게 한다.
- [x] 165. 불러오는 동안 화면 높이가 심하게 흔들리지 않게 한다.
- [x] 166. 음식 이름이 길어도 카드가 깨지지 않게 한다.
- [x] 167. 서버 오류 뒤에도 입력 내용을 잃지 않고 다시 시도하게 한다.

## 9. 기록과 비밀번호 보호

목표: 데모의 한계를 알리고 비밀키와 기록을 안전하게 다룬다.

- [x] 168. `meals`, `weights` 테이블에 RLS를 활성화한다. (RLS는 데이터베이스 접근 제한으로, 아무나 기록을 건드리지 못하게 함)
- [x] 169. 브라우저용 anon key에는 테이블 직접 읽기·쓰기 정책을 부여하지 않는다.
- [x] 170. 데이터베이스 작업은 Python API에서만 하고 service role key는 서버 환경 변수에서만 읽는다.
- [x] 171. 모든 서버 주소에서 입력 내용을 다시 검사한다.
- [x] 172. 복잡한 오류 대신 사용자가 이해할 수 있는 오류 문구를 보여 준다.
- [x] 173. 로그에 음식 내용 같은 사용자 입력을 불필요하게 남기지 않는다.
- [x] 174. 오류 기록에서 비밀정보와 개인정보를 지운다.
- [x] 175. service role key가 브라우저 번들·Git 기록·로그에 없는지 검사한다. (서버 비밀키가 공개되지 않았는지 확인)
- [x] 176. 로그인 없는 데모는 다른 사람이 같은 기록을 볼 수 있다는 점을 안내문에 적는다.
- [x] 177. 외부 공개 전 로그인 또는 배포 접근 제한이 필요한지 다시 결정한다.
- [x] 178. 데이터베이스 백업·복구 방법을 확인한다.

## 10. 마지막 검사와 배포

목표: 실제 인터넷 환경에서 처음부터 끝까지 정상 동작하는지 확인한다.

- [ ] 179. TypeScript 타입 검사, ESLint, Prettier 검사를 통과한다. (화면 코드의 오류와 형식을 마지막 확인)
- [ ] 180. Python 린트와 테스트를 통과한다. (서버 코드의 오류와 기능을 마지막 확인)
- [ ] 181. 화면 컴포넌트와 사용자 흐름 테스트를 통과한다. (사람이 실제 쓰는 순서대로 확인)
- [ ] 182. 프로덕션 빌드가 성공하는지 확인한다. (인터넷에 올릴 수 있는 웹사이트 파일이 만들어지는지 확인)
- [ ] 183. Supabase 마이그레이션을 실제 프로젝트에 적용한다. (만든 데이터베이스 표를 실제 Supabase에 만든다)
- [ ] 184. Vercel의 개발·미리보기·운영 환경 변수를 분리한다. (테스트용과 실제 서비스용 비밀 설정을 나눈다)
- [ ] 185. Vercel Preview에서 Next.js·Python API·Supabase 연결을 확인한다. (인터넷에 올리기 전 연결이 되는지 확인)
- [ ] 186. HTTPS 사이트에서 보안 경고가 없는지 확인한다.
- [ ] 187. 배포한 뒤 서버 상태 주소(`/api/health`)를 확인한다.
- [ ] 188. 배포 환경에서 날짜 선택→체중 저장→식단 저장→수정→재확인을 검사한다. (실제 사용 순서가 끝까지 되는지 확인)
- [ ] 189. 배포 환경에서 달 이동→캐릭터 확인→날짜 상세→수정 화면 이동을 검사한다. (캘린더 사용 흐름이 끝까지 되는지 확인)

## 첫 버전 완료 기준

- [ ] 190. 날짜별 체중을 입력·저장·수정하고 다시 볼 수 있다. (체중 기능의 최종 목표)
- [ ] 191. 잘못된 체중은 저장되지 않는다.
- [ ] 192. 네 끼 식사를 각각 입력하고 저장할 수 있다. (식단 기능의 최종 목표)
- [ ] 193. 같은 날짜의 같은 식사를 다시 저장하면 중복 없이 수정된다.
- [ ] 194. 카드 색이 클린식·자유식 상태와 맞다.
- [ ] 195. 날짜를 바꿔도 해당 날짜 기록을 다시 볼 수 있다.
- [ ] 196. 월별 달력에 식단 상태에 맞는 캐릭터가 보인다. (클린식은 천사, 자유식 포함은 악마)
- [ ] 197. 자유식이 하나라도 있으면 악마 캐릭터가 보인다.
- [ ] 198. 식단이 없는 날에는 캐릭터가 보이지 않는다.
- [ ] 199. 기록한 날짜를 누르면 체중과 네 끼 기록을 볼 수 있다.
- [ ] 200. 빈 날짜를 누르면 빈 안내와 기록하기 버튼을 볼 수 있다.
- [ ] 201. 수정하기를 누르면 선택한 날짜의 기록 화면으로 간다.
- [ ] 202. 휴대폰과 PC에서 핵심 기능을 사용할 수 있다.

## 첫 버전에는 하지 않는 나중 기능

- [ ] 203. 회원가입·로그인과 사용자별 기록 분리 (사람마다 자기 기록만 보게 하는 나중 기능)
- [ ] 204. 식단 기록 삭제
- [ ] 205. 칼로리·영양성분 자동 계산
- [ ] 206. 체중 변화 그래프
- [ ] 207. 월별 클린식 통계와 달성률
- [ ] 208. 연속 클린식 기록
- [ ] 209. 식단 사진 올리기 (먹은 음식 사진을 기록에 붙이는 나중 기능)
- [ ] 210. AI 식단 분석과 추천 (AI가 식단을 분석해 주는 나중 기능)
- [ ] 211. 친구 기능과 SNS 공유 (다른 사람과 기록을 나누는 나중 기능)
