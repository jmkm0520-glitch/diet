# 포트폴리오 작성 계획

> 최종 포트폴리오에 **무엇을 어떤 순서로 쓸지** 정리한 문서.
> 완성본이 아니라 뼈대 + 이미 쓸 수 있는 부분의 초안이다. 나중에 노션으로 옮길 때 이 순서 그대로 쓰면 된다.

작성일: 2026-08-20

## 문서 3개의 역할

| 문서                                | 역할                                            |
| ----------------------------------- | ----------------------------------------------- |
| `portofolio subject.md`             | 과제 요구사항 (기준)                            |
| `portofolio WORK_LIST.md`           | 할 일 체크리스트                                |
| `docs/portofolio service-review.md` | **재료 창고** — 1부 개선 전 분석, 2부 작업 기록 |
| **`portofolio.md`** (이 파일)       | **재료를 어떤 이야기로 엮을지**                 |

포트폴리오를 쓸 때는 이 파일의 순서를 따라가면서, 각 절이 가리키는 재료를 `service-review.md`에서 꺼내 오면 된다.

---

## 전체 구조와 진행 상태

| #   | 섹션                                    | 재료 출처    | 상태                     |
| --- | --------------------------------------- | ------------ | ------------------------ |
| 0   | 표지 — 한 줄 소개, 링크                 | —            | ✅ 초안 있음             |
| 1   | 프로젝트 소개 · 제작 배경               | review 1·2장 | ✅ 초안 있음             |
| 2   | 주요 기능                               | review 4장   | ✅ 초안 있음             |
| 3   | 기술 스택                               | review 6장   | ✅ 그대로 사용           |
| 4   | 시스템 구조                             | review 7장   | ✅ 그대로 사용           |
| 5   | 데이터 구조                             | review 5장   | ✅ 그대로 사용           |
| 6   | 사용자 흐름                             | 직접 작성    | ✅ 완료                  |
| 7   | 개선 과정 — 문제 발견 → 우선순위        | review 8장   | ✅ 7건·우선순위 확정     |
| 8   | Before / After                          | 7장의 HIGH 3 | 🟡 항목 확정, After 대기 |
| 9   | **신규 기능 — 통계 · 차트 · 주간 요약** | —            | ⬜ **본편, 미착수**      |
| 10  | Trouble Shooting                        | review 2부   | ✅ 4건 준비              |
| 11  | 테스트                                  | 아래 숫자    | ✅ 초안 있음             |
| 12  | 배포                                    | review 7장   | ✅ 그대로 사용           |
| 13  | 회고 · 향후 개선                        | —            | ⬜ 마지막에              |

**핵심**: 9번이 비어 있으면 포트폴리오의 무게가 절반이다. UI 수정만 있는 포트폴리오와, 데이터를 집계해 정보로
바꾼 포트폴리오는 다르게 읽힌다.

---

## 0. 표지

```
오늘도 가볍게 — 식단 기록 서비스

칼로리 계산 없이, 매일 먹은 음식을 한 줄로 기록하고 패턴을 확인하는 개인 식단 기록 서비스.
기존에 운영하던 서비스를 직접 사용하며 문제를 찾아 UX를 개선하고,
쌓인 기록을 집계하는 통계 기능을 추가했다.

배포   https://www.food-broccoli.shop
GitHub https://github.com/jmkm0520-glitch/diet
기간   (시작일) ~ 2026-08-__
역할   기획 · 디자인 · 프론트엔드 · 백엔드 · 배포 (1인)
```

---

## 1. 프로젝트 소개 · 제작 배경

**쓸 내용** — `service-review.md` 1·2장에서 가져온다.

- 서비스 목적: 기록의 간편함. 입력은 음식 이름 + `클린식/자유식` 두 가지뿐.
- **"하지 않는 것" 표**를 반드시 넣을 것. 칼로리 계산·점수화·목표 체중 추적을 의도적으로 뺐다는 점이
  이 서비스의 성격을 가장 잘 보여준다. 기능을 더한 것보다 **덜어낸 판단**이 설득력이 있다.
- 타겟 사용자: "복잡한 칼로리 계산보다 매일 먹은 음식을 간단히 기록하고 패턴을 확인하고 싶은 사용자"
  - 구체 조건 4가지.

---

## 2. 주요 기능

`service-review.md` 4장 그대로. 화면 캡쳐를 옆에 붙인다.

| 기능              | 설명                                         |
| ----------------- | -------------------------------------------- |
| 회원가입 · 로그인 | 이메일 인증, 쿠키 세션, 사용자별 데이터 분리 |
| 식단 기록         | 아침·점심·저녁·간식 4칸, 클린식/자유식 분류  |
| 체중 기록         | 날짜별 1건                                   |
| 캘린더            | 월 단위로 클린/자유 패턴과 체중 확인         |
| CSV 가져오기      | 기존 다이어트일지를 한 번에 이관             |
| **식단 통계**     | ⬜ 이번에 추가                               |

---

## 3~5. 기술 스택 · 시스템 구조 · 데이터 구조

`service-review.md` 6·7·5장을 **거의 그대로** 옮기면 된다. 이미 버전·컬럼·제약까지 다 확인해 놨다.

특히 강조할 두 가지:

1. **프론트는 TypeScript, 백엔드는 Python** — 한 저장소에서 Next.js가 `src/`를, Vercel Python 런타임이
   `api/`를 담당하는 구조.
2. **회원 분리를 서버 코드가 보장** — RLS를 켜고 `anon`/`authenticated` 권한을 회수한 뒤,
   모든 질의에 `member_id`를 붙인다. `NEXT_PUBLIC_` 환경변수가 하나도 없어 Supabase 키가
   클라이언트 번들에 들어가지 않는다. **보안 설계를 설명할 수 있는 좋은 소재다.**

---

## 6. 사용자 흐름 ✅

> 실제 화면과 코드(`AuthGate.tsx`, `MealCard.tsx`, `CalendarView.tsx`, `api/*.py`)를 대조해 정리했다.
> 로그인이 필요한 화면이라 자동 클릭 테스트는 하지 못했고, 코드 경로와 캡쳐로 확인했다.

### Flow 1 — 로그인 / 회원가입

```
사이트 접속
   │
   ▼
GET /api/authentication?action=session   ← 쿠키로 세션 확인
   │
   ├─ 세션 있음 ──────────────────▶ 오늘 기록 화면
   │
   └─ 세션 없음 ──▶ 로그인 카드 (before-04-login.png)
                        │
                        ├─ 로그인 ──▶ POST ?action=login
                        │              쿠키 2개 발급 → 오늘 기록 화면
                        │
                        └─ 회원가입 ──▶ POST ?action=signup
                                          │  (before-10-signup.png)
                                          ▼
                                       인증 메일 발송 · verify 모드 전환
                                       이메일을 sessionStorage에 보관
                                          │
                                          ▼
                                       6자리 인증번호 입력
                                       POST ?action=verify_email
                                          │
                                          ▼
                                       가입 완료 → 오늘 기록 화면
```

**설명 포인트**

- 세션 토큰(`diet_access_token`, `diet_refresh_token`)은 **HTTP 쿠키**로만 오간다. 브라우저 JS가 토큰을
  직접 들고 있지 않다.
- 가입 도중 새로고침해도 `sessionStorage`에 이메일이 남아 인증 단계로 복귀한다.
- 로그인 전에는 `AuthGate`가 children을 렌더하지 않으므로 `/calendar`, `/import`에 직접 접근해도
  로그인 카드가 나온다.

### Flow 2 — 식단 기록

```
오늘 기록 화면
   │
   ▼
GET /api/day?date=YYYY-MM-DD   ← 그 날의 4끼 + 체중
   │
   ▼
끼니 카드에 음식 입력 → 클린식 / 자유식 선택
   │
   ▼
저장 버튼
   │
   ├─ 음식 입력함 ──▶ 입력값 + 선택한 분류
   └─ 음식 비어 있음 ─▶ "공복" + 클린식 (자동)
   │
   ▼
PUT /api/meal   { date, meal, food, type }
   │
   ▼
upsert (member_id, date, meal)   ← 같은 칸은 덮어쓰기
   │
   ▼
카드 잠금 · "○○ 식단이 저장되었습니다."
   │
   └─ 입력칸이나 분류 버튼을 건드리면 → 잠금 해제 (수정 버튼 불필요)
```

**설명 포인트**

- 저장 키가 `(회원, 날짜, 끼니)` 유니크라 **행이 쌓이지 않고 덮어쓴다.** 같은 끼니를 열 번 고쳐도 1행이다.
- 네트워크 실패(404) 시 `localStorage`에 저장해 입력을 잃지 않는다.

### Flow 3 — 기록 확인 (캘린더)

```
캘린더 탭
   │
   ▼
GET /api/calendar?year=&month=
   │
   ▼
날짜별 status 계산 (서버, api/calendar.py)
   │  자유식이 1건이라도 있으면        → free  (🍄 빨강)
   │  기록이 있고 전부 클린식이면      → clean (🥦 초록)
   │  기록 없음                        → 표시 없음
   ▼
달력 셀에 이모지 + 체중 표시   (before-02-calendar.png)
   │
   ▼
날짜 클릭 → GET /api/day?date=
   │
   ▼
상세 모달: 4끼 각각의 음식과 분류  (before-03-calendar-detail.png)
   │
   ├─ 수정하기 ──▶ 오늘 기록 화면으로 이동 (해당 날짜)
   └─ 초기화   ──▶ DELETE /api/day?date=  (식단 + 체중 전부)
```

---

## 7. 개선 과정 — 문제 발견과 우선순위 ✅

직접 사용하고 코드를 대조하며 UX 문제 **7건**을 찾았다. 전부 고치는 대신 우선순위를 매겨
**HIGH 3건을 이번 개선 대상으로 확정**했다.

### 판단 기준

| 축                       | 질문                                      |
| ------------------------ | ----------------------------------------- |
| **서비스 목적과의 거리** | 이 문제가 "식단 패턴 확인"을 직접 막는가? |
| **마주치는 빈도**        | 매일 겪는가, 어쩌다 한 번인가?            |
| **회복 가능성**          | 사용자가 스스로 우회할 수 있는가?         |

### 문제 목록과 우선순위

| #   | 문제                                                                                              | 근거                                                  | 우선순위 | 개선 대상 |
| --- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | -------- | --------- |
| 1   | 통계·차트·주간 요약이 없다. 패턴을 보려면 캘린더 색을 눈으로 세야 한다                            | `before-02-calendar.png` — 한 달이 빨강·초록으로 가득 | **HIGH** | ✅        |
| 2   | 캘린더가 기록하지 않은 끼니를 판정에서 제외한다. 2끼만 기록한 날과 4끼 다 기록한 날이 똑같이 초록 | `before-08` + `before-03` (6/10은 점심·저녁만 기록)   | **HIGH** | ✅        |
| 3   | 회원가입 제목이 `첫 회원 만들기`. 멀티 유저인데 1인 시절 문구가 남았다                            | `before-10-signup.png`, `AuthGate.tsx:194`            | **HIGH** | ✅        |
| 4   | 조회 실패 안내가 캘린더 상세에만 있고 오늘 기록 화면에는 없다                                     | `page.tsx` 로드 실패 시 무음                          | MEDIUM   | 여력되면  |
| 5   | 끼니별 삭제가 없다. 한 끼 실수에 하루 전체를 지워야 한다                                          | `식단 초기화`가 유일한 삭제 수단                      | MEDIUM   | ⬜        |
| 6   | CSV 가져오기 화면이 파일 선택 전 대부분 빈 공간                                                   | `before-09-import.png`                                | LOW      | ⬜        |
| 7   | CSV 가져오기가 특정 열 서식만 지원                                                                | `spreadsheetImport.ts` 헤더 검증                      | LOW      | ⬜        |

### 우선순위를 그렇게 매긴 이유

**1번이 HIGH인 이유** — 서비스 목적이 "패턴 확인"인데 그 수단이 통째로 없다. 세 기준을 모두 만족한다:
목적과 직결되고, 매번 겪고, 사용자가 우회할 방법이 없다(눈으로 세는 것 말고는).

**2번이 HIGH인 이유** — 화면이 **틀린 정보를 주기** 때문이다. 없는 기능(1번)보다 잘못된 정보가 더 나쁘다.
2끼만 기록한 날을 "클린한 하루"로 읽게 만든다.

**3번이 HIGH인 이유** — 고치는 비용이 한 줄인데, 신규 사용자가 **가장 먼저 보는 화면**이다.
비용 대비 효과가 가장 크다.

**5번을 MEDIUM에 둔 이유** — 실제로 오늘 이 기능을 만들었다가 되돌렸다. 카드에 버튼이 하나 더 붙는
것보다 `저장 → 수정` 두 상태를 유지하는 쪽이 낫다고 판단했다. 빈도도 낮다(잘못 저장하는 일은 드물다).

---

## 8. Before / After ✅ 항목 확정

7번에서 고른 HIGH 3건을 그대로 Before/After 3항목으로 쓴다.

| #   | 항목             | Before                                                        | After                                   |
| --- | ---------------- | ------------------------------------------------------------- | --------------------------------------- |
| 1   | 식단 패턴 파악   | 캘린더에서 🥦🍄 색을 눈으로 세야 함 (`before-02`)             | ⬜ 통계 화면 — 7일 클린/자유 비율, 차트 |
| 2   | 하루 상태 판정   | 2끼만 기록해도 "클린한 하루"로 표시 (`before-08`+`before-03`) | ⬜ 기록률을 함께 표시                   |
| 3   | 신규 가입 첫인상 | 제목이 `첫 회원 만들기` (`before-10`)                         | ⬜ 멀티 유저에 맞는 문구                |

캡쳐는 **개선 후 같은 구도로 다시 찍어야** 비교가 된다.

---

## 9. 신규 기능 — 통계 · 차트 · 주간 요약 ⬜ **본편**

아직 착수 전. 완성 후 다음을 기록한다.

- 무엇을 집계하는가 (최근 7일, 클린/자유 비율, 기록률)
- **어디서 집계하는가** — DB 질의 / Python / 브라우저 중 어디서 계산했고 왜 그렇게 정했는지
- 차트 선택 근거 (어떤 형태가 이 데이터에 맞는가)
- 주간 요약 문장 생성 규칙
- 데이터가 적을 때(기록 1~2일) 어떻게 보여주는가 ← **면접에서 자주 묻는 지점**

---

## 10. Trouble Shooting ✅

`service-review.md` 2부에 4건이 `증상 / 원인 / 해결 / 확인 / 한 줄` 형식으로 정리돼 있다. 그중 2건은
특히 값이 크다.

**(1) 증상과 원인이 달랐던 사례**
"새로고침하면 자동으로 기록이 생긴다"는 증상이었지만, 실제로는 빈 칸 저장 시 코드가 `공복`을 채워
**DB에 진짜 행을 만든 것**이었다. 코드를 고치기 전에 데이터의 출처부터 확인해야 했다.
검증도 빌드 성공 표시가 아니라 **배포된 JS 번들을 직접 받아 문자열을 확인**했다.

**(2) 같은 버그를 4번 고치려다 실패한 사례**
햄버거 버튼이 스크롤을 따라다니는 문제를 네 번 수정했지만(`c5f41b0`, `720a087`, `7962ee3`, `cfffe4e`)
모두 `position: fixed`를 유지한 채 좌표만 조정한 것이었다. 다섯 번째에 **버튼을 헤더 DOM 안으로 옮기는**
구조 변경으로 한 번에 해결됐다.
→ _증상을 고치는 것과 원인을 고치는 것의 차이._ 커밋 히스토리가 그대로 증거가 된다.

---

## 11. 테스트 ✅

| 구분       | 도구                                   | 개수                     |
| ---------- | -------------------------------------- | ------------------------ |
| 백엔드     | `pytest`                               | **86개 통과**            |
| 프론트엔드 | `node:test`                            | 8개 파일 **31개 케이스** |
| 정적 검사  | `tsc --noEmit`, ESLint, Prettier, ruff | 통과                     |

**설명할 포인트**: 프론트엔드의 순수 로직을 UI에서 분리해 `.ts` 모듈로 두고 테스트한다
(`mealCardState.ts`, `calendarGrid.ts`, `spreadsheetImport.ts`). 화면을 띄우지 않고 규칙만 검증하기 위한 구조다.

---

## 12. 배포 ✅

`service-review.md` 7장. GitHub `main` push → Vercel 자동 배포 → Supabase.
환경변수는 Sensitive로 등록해 값을 다시 읽을 수 없다.

---

## 13. 회고 · 향후 개선 ⬜

마지막에 작성. 되돌린 판단(끼니별 삭제 기능을 만들었다가 UI 단순함을 위해 제거)도 함께 쓰면
"넣는 판단만큼 빼는 판단도 했다"는 인상을 준다.

---

# 필요한 캡쳐 목록

## Before 기준선 — 10장 확보 완료 ✅

전부 `docs/images/`에 저장돼 있다. 2026-08-20 상태이며, 이 화면들이 포트폴리오의 Before다.

| 파일                            | 화면                      | 쓸 곳                     |
| ------------------------------- | ------------------------- | ------------------------- |
| `before-01-today-empty.png`     | 오늘 기록 (기록 없는 날)  | 2번 주요 기능             |
| `before-02-calendar.png`        | 캘린더 2026년 6월         | **8번 Before/After** ⭐   |
| `before-03-calendar-detail.png` | 날짜 상세 모달            | 8번 Before/After          |
| `before-04-login.png`           | 로그인                    | 2번 주요 기능             |
| `before-05-sidemenu.png`        | 사이드 메뉴               | 2번 주요 기능             |
| `before-06-mobile-calendar.png` | 모바일 캘린더             | 모바일 반응형 증빙        |
| `before-07-mobile-today.png`    | 모바일 오늘 기록          | 모바일 반응형 증빙        |
| `before-08-today-filled.png`    | 오늘 기록 (6월 10일)      | 2번 주요 기능 ⭐          |
| `before-09-import.png`          | CSV 가져오기              | 2번 주요 기능             |
| `before-10-signup.png`          | 회원가입 "첫 회원 만들기" | **7·8번 UX 문제 증거** 🔴 |

**가장 값이 큰 3장**

- `before-02` — 한 달이 빨강·초록으로 꽉 차 있다. "패턴을 보려면 눈으로 세야 한다"가 그림 하나로 설명된다.
- `before-08` — 6월 10일은 점심·저녁만 기록됐는데 캘린더에서는 그 날 전체가 클린식(초록)이다.
  `before-03`과 나란히 놓으면 "하루에 상태 하나만 표시하는" 문제의 증거가 된다.
- `before-10` — 제목이 `첫 회원 만들기`. 멀티 유저인데 1인 시절 문구가 남아 있다.

## 개선 후 (After)

개선한 화면마다 `before-*`와 **같은 구도로** 찍어야 비교가 된다.

| 파일명 제안           | 화면                       |
| --------------------- | -------------------------- |
| `after-01-stats.png`  | 통계 화면 전체             |
| `after-02-chart.png`  | 차트                       |
| `after-03-weekly.png` | 주간 요약                  |
| `after-04-nav.png`    | 헤더 — 통계 탭이 생긴 모습 |
| `after-05-mobile.png` | 모바일 폭 (통계 포함)      |
| (UX 개선 3건)         | ⬜ 7번에서 확정한 뒤 추가  |

저장 위치는 `docs/images/` 를 권합니다 (기존에 `docs/learning/images/` 전례가 있습니다).
파일만 넣어주시면 문서 안의 알맞은 자리에 제가 연결하겠습니다.

---

# 작성 순서 (권장)

1. **통계 기능 개발** — 9번 섹션의 재료가 생긴다. 이게 없으면 나머지를 아무리 잘 써도 약하다.
2. **캡쳐 정리** — After 화면들.
3. **7·8번 확정** — UX 문제 우선순위와 Before/After 표.
4. **3·4·5·11·12번 조립** — 이미 재료가 다 있어서 옮기기만 하면 된다.
5. **6번 사용자 흐름 다이어그램.**
6. **0·1·2·13번** — 표지·소개·회고. 전체를 다 만든 뒤에 써야 톤이 맞는다.
7. **노션으로 이관** — 캡쳐가 많은 발표용 버전.
